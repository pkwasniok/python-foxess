[project]
name = "mqtt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-foxess",
    "paho-mqtt>=2.1.0",
    "python-dotenv>=1.2.2",
]

[tool.uv.sources]
python-foxess = { workspace = true }

